package com.plantogether.destination.security;

import com.plantogether.common.security.AbstractDeviceIdFilterTest;

/** Security regression test — verifies DeviceIdFilter is active in destination-service (R01). */
class DeviceIdFilterTest extends AbstractDeviceIdFilterTest {}
